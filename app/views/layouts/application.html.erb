<!DOCTYPE html>
<html>
<html lang="ja">
  <head>
    <title>Jobwin</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header class="site-header">
      <div class="site-header__brand">
        <%= link_to "Jobwin", root_path %>
        <span>習慣・タスク・プロジェクトの一元管理</span>
      </div>
      <div class="site-header__actions">
         <% if user_signed_in? %>
          <div class="user_nav grid-6">
            <%= link_to "ログアウト", destroy_user_session_path, data: { turbo_method: :delete },class: "btn btn--ghost" %>
            <%= link_to "マイページ", edit_user_path(current_user.id) , class: "btn" %>
          </div>
         <% else %>
          <div class="grid-6">
        <%= link_to "ログイン", new_user_session_path, class: "btn btn--ghost" %>
        <%= link_to "新規登録", new_user_registration_path, class: "btn" %>
      </div>
      <% end %>
      </div>
    </header>
    <%= yield %>
  </body>
</html>